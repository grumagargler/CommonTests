// Описание:
// Удаляет строки в таблице
//
// Параметры:
// Объект тестируемой таблицы

СтандартнаяОбработка = ложь;

если ( типзнч ( _ ) = Тип ( "Строка" ) ) тогда
	таблица = Фокус ( _ );
иначе
	таблица = _;
конецесли;

Пока Истина Цикл
	_.ПерейтиКПервойСтроке ();
	строки = _.ПолучитьВыделенныеСтроки ();
	если ( строки.Количество ()  = 0 ) тогда
		прервать;
	конецесли;
	_.УдалитьСтроку ();
КонецЦикла;


