// Описание:
// Возвращает количество строк в таблице
//
// Параметры:
// Объект тестируемой таблицы

СтандартнаяОбработка = ложь;

колво = 0;
_.ПерейтиКПервойСтроке ();
строки = _.ПолучитьВыделенныеСтроки ();
если ( строки.Count () > 0 ) тогда // Обход проблемы падения платформы
	_.ВыделитьВсеСтроки ();
	колво = _.ПолучитьВыделенныеСтроки ().Количество ();
конецесли;

возврат колво;
