СтандартнаяОбработка = ложь;

п = новый Структура ();
п.Вставить ( "Где" );
п.Вставить ( "Что" );
п.Вставить ( "Кнопка", "!СписокНайти" );
п.Вставить ( "Как", 1 ); // 0 - По началу строки, 1 - по точному совпадению, 2 - По части строки
возврат п;
